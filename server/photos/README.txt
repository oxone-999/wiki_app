Drop her photos in THIS folder (.jpg .jpeg .png .gif .webp .avif).

They automatically appear:
  - as pink polaroids inside wiki sections (attach them per-section in /admin)
  - as planes in the 3D photo universe at /gallery (all photos show automatically)
  - in the /admin photo picker

No rebuild needed — just drop files here and refresh the page.
The birthday gate's center photo uses a file named:  hero.jpg
(put a hero.jpg here for her main gothic-frame portrait)
